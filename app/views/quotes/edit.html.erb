<div class="container">
  <h1>Edit Quote</h1>
  
  <%= render 'form', quote: @quote %>
  
  <%= link_to 'Show', @quote, class: 'btn btn-secondary' %>
  <%= link_to 'Back', quotes_path, class: 'btn btn-secondary' %>
</div>